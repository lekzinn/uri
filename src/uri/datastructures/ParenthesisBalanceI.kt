package uri.datastructures

import java.util.*


//TODO https://www.urionlinejudge.com.br/judge/en/problems/view/1068
fun main(args: Array<String>) {
    val scan = Scanner(System.`in`)
    while(scan.hasNextLine()){
        val value = scan.nextLine()
        val stack = Stack<Int>();
    }
}